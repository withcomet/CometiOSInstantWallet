import XCTest
@testable import comet_ios_instant_wallet

final class comet_ios_instant_walletTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(comet_ios_instant_wallet().text, "Hello, World!")
    }
}
